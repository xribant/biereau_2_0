{%  extends "base.html.twig" %}

{% block title %}Biereau.be - Inscription{% endblock %}

{% block content %}

    <section class="hero-wrap hero-wrap-2" style="background-image: url('images/bg_2.jpg');">
        <div class="overlay"></div>
        <div class="container">
            <div class="row no-gutters slider-text align-items-center justify-content-center">
                <div class="col-md-9 ftco-animate text-center">
                    <h1 class="mb-2 bread">Inscription</h1>
                    <p class="breadcrumbs">
                        <span class="mr-2"><a href="{{ path('home.index') }}">Accueil <i class="ion-ios-arrow-forward"></i></a></span>
                        <span>Infos Pratiques <i class="ion-ios-arrow-forward"></i></span>
                        <span>Inscription <i class="ion-ios-arrow-forward"></i></span>
                    </p>
                </div>
            </div>
        </div>
    </section>

    <section class="ftco-section bg-light">
        <div class="container">
            <div class="row justify-content-center mb-1 pb-1">
                <div class="col-md-8 text-center heading-section ftco-animate">
                    {% for message in app.flashes('success') %}
                        <div class="alert alert-success">
                            {{ message|raw }}
                        </div>
                    {% endfor %}
                    <p>Chaque année, le collège du Biéreau organise deux séances d'informations permettant aux parents de recevoir documents et renseignements nécessaires
                    en vue de l'inscription de leur(s) enfant(s) dans l'établissement.</p>
                </div>
            </div>

        </div>
    </section>

    <section class="ftco-section ftco-consult ftco-no-pt ftco-no-pb" style="background-image: url(images/bg_6.jpg);" data-stellar-background-ratio="0.5">
        <div class="container">
            <div class="row justify-content-end">
                <div class="col-md-6 py-5 px-md-5 bg-primary">

                    {% if academicYear.activeforregistration == 1 %}
                    <div class="heading-section heading-section-white ftco-animate mb-5">
                        <h2 class="mb-4">Préinscription {{ academicYear.year }}</h2>
                        <p>Veuillez remplir le formulaire afin de vous inscrire à l'une des deux séances d'information. Une  confirmation vous sera envoyée par e-mail.</p>
                    </div>
                    {{ form_start(form) }}
                    <div class="appointment-form ftco-animate">
                        <div class="d-md-flex">
                            <div class="form-group">
                                {{ form_row(form.parentLastName, { 'attr' : { 'class': 'form-control', 'placeholder': 'Nom du parent' } }) }}
                            </div>
                            <div class="form-group ml-md-4">
                                {{ form_row(form.parentFirstName, { 'attr' : { 'class': 'form-control', 'placeholder': 'Prénom du parent' } }) }}
                            </div>
                        </div>
                        <div class="d-md-flex">
                            <div class="form-group">
                                {{ form_row(form.parentPhone, { 'attr' : { 'class': 'form-control', 'placeholder': 'Téléphone' } }) }}
                            </div>
                            <div class="form-group ml-md-4">
                                {{ form_row(form.parentEmail, { 'attr' : { 'class': 'form-control', 'placeholder': 'E-Mail' } }) }}
                            </div>
                        </div>
                        <div class="d-md-flex">
                            <div class="form-group">
                                {{ form_row(form.childLastName, { 'attr' : { 'class': 'form-control', 'placeholder': 'Nom de l\'enfant' } }) }}
                            </div>
                            <div class="form-group ml-md-4">
                                {{ form_row(form.childFirstName, { 'attr' : { 'class': 'form-control', 'placeholder': 'Prénom de l\'enfant' } }) }}
                            </div>
                        </div>
                        <div class="d-md-flex">
                            <div class="form-group appointment-form">
                                {{ form_row(form.childBirthDate) }}
                            </div>
                            <div class="form-group ml-md-4">
                                <div class="form-field">
                                    <div class="select-wrap">
                                        <div class="icon"><span class="ion-ios-arrow-down"></span></div>
                                        {{ form_row(form.childEntryDate) }}
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="d-md-flex">
                            <div class="form-group">
                                <div class="form-field">
                                    <div class="select-wrap">
                                        <div class="icon"><span class="ion-ios-arrow-down"></span></div>
                                        {{ form_row(form.childSection) }}
                                    </div>
                                </div>
                            </div>
                            <div class="form-group ml-md-4">
                                <div class="form-field">
                                    <div class="select-wrap">
                                        <div class="icon"><span class="ion-ios-arrow-down"></span></div>
                                        {{ form_row(form.sessionDate) }}
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="d-md-flex">

                            <div class="form-group">
                                <input type="submit" value="Confirmer l'inscription" class="btn btn-secondary py-3 px-4">
                            </div>
                        </div>
                    </div>
                    {{ form_end(form) }}
                    {% else %}
                    <div class="heading-section heading-section-white ftco-animate mb-5">
                        <h2 class="mb-4">Préinscription {{ academicYear.year }}</h2>
                        <p>Les inscriptions pour cette année académique ne sont pas encore ouvertes.</p>
                        <p>Nous vous invitions à revenir le {{ academicYear.startRegistrationDate|format_datetime('full', 'none', locale='fr') }} à partir de {{ academicYear.startRegistrationDate|format_datetime('none', 'short', locale='fr') }} pour l'ouverture des inscriptions.</p>
                    </div>
                    {% endif %}

                </div>
            </div>
        </div>
    </section>
{% endblock %}